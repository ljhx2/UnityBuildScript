# 🚀 Unity Automated Build Suite

Unity 프로젝트의 빌드 설정을 자동화하고, 플랫폼별 최적화된 빌드 프로세스를 제공하는 에디터 스크립트 모음입니다. **Addressables, Player(Desktop), WebGL** 빌드를 독립적인 모듈로 분리하여 관리 효율성을 높였습니다.

---

## 📂 파일 구성 및 역할

모든 스크립트는 `Assets/Editor` 폴더 내에 위치해야 하며, 각 플랫폼 및 목적에 맞는 자동 세팅과 빌드 기능을 수행합니다.

| 파일명 | 설명 |
| :--- | :--- |
| **`ExecuteBuild_Addressable.cs`** | Addressable Assets의 설정 자동화 및 콘텐츠 빌드 |
| **`ExecuteBuild_Player.cs`** | 일반 데스크톱(PC/Mac/Linux) 환경을 위한 빌드 자동화 |
| **`ExecuteBuild_WebGL.cs`** | WebGL 타겟 전용 설정 적용 및 빌드 (**WebGL 타겟 시에만 노출**) |

---

## ✨ 핵심 기능

### 1. 자동화된 빌드 세팅 (Auto-Settings)
* 각 스크립트 실행 시 플랫폼에 맞는 `PlayerSettings`, `Build Options`, `Compression` 방식 등을 자동으로 구성합니다.
* 수동 설정으로 인한 휴먼 에러를 방지합니다.

### 2. 조건부 메뉴 활성화 (Context-Aware Menu)
* **`ExecuteBuild_WebGL.cs`**는 사용자의 편의를 위해 Unity 에디터의 **Build Target이 WebGL로 설정된 상태**에서만 상단 메뉴에 나타납니다.

### 3. 모듈형 워크플로우
* **Addressable Build:** 리소스 업데이트가 필요할 때 독립적으로 에셋 번들을 빌드합니다.
* **Player Build:** 최종 사용자용 실행 파일을 생성합니다.
* **WebGL Build:** 웹 환경에 최적화된 빌드 설정을 적용하여 결과물을 생성합니다.

---

## 🛠 사용 방법

### 1. 설치
`Assets/Editor` 폴더를 생성하고, 본 저장소의 `.cs` 파일들을 해당 폴더 안에 복사합니다.

### 2. 메뉴 실행
Unity 상단 메뉴바에서 다음 항목을 선택하여 빌드를 진행합니다.
* `Build` > `Addressables` > `Build Content`
* `Build` > `Player` > `Build Desktop`
* `Build` > `WebGL` > `Build for Web` *(WebGL 타겟일 때만 메뉴 표시)*

### 3. CLI 빌드 (명령행 인터페이스)
CI/CD 환경에서 다음과 같이 명령어로 빌드를 자동화할 수 있습니다.

```bash
# WebGL 빌드 실행 예시
Unity.exe -batchmode -nographics -executeMethod ExecuteBuild_WebGL.Build -quit
